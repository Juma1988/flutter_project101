import 'package:flutter/material.dart';

class Task01 extends StatelessWidget {
  const Task01({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "hello world",
        style: TextStyle(color: Colors.green, decoration: TextDecoration.none),
      ),
    );
  }
}
