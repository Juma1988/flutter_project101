import 'package:flutter/material.dart';

class Task09 extends StatelessWidget {
  const Task09({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      floatingActionButton: Icon(Icons.cast)
    );
  }
}
